// Lab_4_4.cpp
// < Мощук Мар'яна >
// Лабораторна робота № 4.4
// Табуляція функції, заданої графіком.
// Варіант 9

#include <iostream>
#include <iomanip>
#include <cmath> 
#include <functional>
using namespace std;

int main()
{
    setlocale(LC_CTYPE, "ukr");
    double x, xp, xk, dx, a, y;
    cout << "Введiть значення a: "; cin >> a;
    cout << "xp = "; cin >> xp;
    cout << "xk = "; cin >> xk;
    cout << "dx = "; cin >> dx;
    cout << fixed;
    cout << setw(10) << "x" << setw(10) << "f(x)" << endl;
    cout << "---------------------" << endl;

    for (x = xp; x <= xk; x += dx)
    {
        if (x <= -7)
            y = 0;
        else
            if (-7 < x && x <= -3)
                y = x + 7;
            else
                if (-3 < x && x <= -2)
                    y = 4;
                else
                    if (-2 < x && x <= 2)
                        y = x * x;
                    else
                        if (2 < x && x <= 4)
                            y = 8 - 2 * x;
                        else
                            y = 0;

            cout << setw(10) << fixed << setprecision(2) << x
                << setw(10) << fixed << setprecision(2) << y << endl;
    }
    cout << "---------------------------" << endl;
    return 0;
}
